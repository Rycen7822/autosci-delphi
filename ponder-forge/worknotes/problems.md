# Ponder-Forge Real-Run Problems Ledger

- compressed_at: 2026-07-04T08:05:15+08:00
- source_dir: `/home/xu/project/autosci-delphi/ponder-forge`
- installed_dir: `/home/xu/.hermes/plugins/ponder_forge`
- real_task_path: `/home/xu/project/loop/DeepScientist/quests/001`
- guardrail: read-only STAGE10 analysis; do not modify quest code or documents.
- final closeout commit before worknote compression: `38b926e fix: harden ponder-forge real-run contracts`

## Current status

PF-REAL-001..020 are closed in source and installed copy. PF-REAL-021 is open: prior post-PF-REAL-020 clean rounds used controller-generated reports and did not prove the actual live Hermes subagent lane/reviewer execution chain can complete three consecutive real-task rounds. Live round 01 has now completed lane execution/submission (8 lanes, 40 child reports) and is waiting on 83 live independent reviewers. PF-REAL-022 is downgraded to a non-blocking delayed-observability risk: lane delegation `deleg_edd050a1` returned after ~22 minutes, so the earlier 9-minute collector timeout was not evidence of lost lane results. Final stability confidence is still incomplete until reviewer verdicts, gate/finalize/reconcile, and clean-repeat criteria finish. Failed discovery runs `pf_run_7474232e15cf` and `pf_run_5e634c37434c` remain excluded from any clean streak.

## Compact issue index

| id | trigger | root cause | fix / verification |
|---|---|---|---|
| PF-REAL-001 | Native `ponder_forge_start` failed with Hermes metadata kwargs. | Plugin registered raw one-argument handlers while Hermes passed `task_id`/metadata kwargs. | Added registration adapter; focused/full tests and fresh installed probe passed. Later pure-CLI conversion removed default direct-tool exposure. |
| PF-REAL-002 | Delegation payload omitted real goal/path/constraints. | Delegation context forwarded generic task text but not run goal or constraints. | Injected goal and constraints into child contexts; installed payload contained quest path and read-only constraint. |
| PF-REAL-003 | Final Markdown too thin for complex real task. | Renderer did not project evidence/artifact/verdict trace. | Final renderer now includes compact evidence, artifact, and verifier traces; source/installed tests passed. |
| PF-REAL-004 | Completed runs could be polluted by late child reports. | Report submission mutated completed runs; finalize re-evaluated instead of returning stored result. | Completed runs reject new reports; finalize is idempotent; lifecycle tests and real reruns passed. |
| PF-REAL-005 | Analysis gate had hidden `metric_output.command` requirement. | Child prompts listed evidence types but not profile-specific field requirement. | Delegation context and gate diagnostics expose `metric_output.command`/`exit_code`; tests and installed retry passed. |
| PF-REAL-006 | Alias-shaped report payloads silently dropped evidence. | Report ingest accepted permissive JSON but consumed only exact nested keys. | Narrow alias normalization plus fail-loud validation for unlinked/missing evidence refs; focused/full/installed tests and alias smoke passed. |
| PF-REAL-007 | Full suite depended on ignored worknotes fixture. | Test fixture lived under ignored `worknotes/`. | Moved fixture to tracked `tests/fixtures`; full source and installed suites passed. |
| PF-REAL-008 | Gate metrics reported placeholder coverage values. | `gates.py` returned placeholder coverage and counted gaps as unsupported assertions. | Computes supported/unsupported critical counts and real independent/artifact/final trace coverage; post-fix real rounds showed all coverage `1.0`. |
| PF-REAL-009 | Child delegation context was not enough for parent-submittable reports. | Context lacked compact JSON schema, critical/data_result hints, role duties, and de-duplication. | `delegation.py` emits schema, profile gate hints, role duties, and one evidence line; bundled skill updated; tests/source/installed/real clean rounds passed. |
| PF-REAL-010 | Missing CLI args bypassed JSON error envelope. | `argparse` exited before CLI `_err(...)` wrapper. | `JsonArgumentParser.error()` routes argument errors into JSON envelope while preserving help behavior; installed repro `missing_arg_json=true`. |
| PF-REAL-011 | `reconcile` returned success for unknown run ids. | `reconcile_run()` did not validate `store.get_run(run_id)`. | Unknown run now fails like other commands; installed repro showed `reconcile_unknown_success=false`. |
| PF-REAL-012 | `status` asked for `finalize` after completed/final run. | `cmd_status()` derived next action from gate state only. | Status returns `run_status` and terminal `next_required_action="complete"` for completed runs with final artifacts. |
| PF-REAL-013 | Analysis gate accepted failed metric command evidence. | Gate checked non-empty `command` but ignored missing/nonzero `exit_code`. | Analysis gate requires a successful `metric_output` (`exit_code=0`); installed repro blocked `exit_code=1` and passed `exit_code=0`. |
| PF-REAL-014 | Non-analysis delegation/retry contracts were analysis-shaped or thin. | Generic contract used placeholder assertion type and analysis evidence examples; reconcile had separate thin retry prompt. | Child contexts now derive profile-specific assertion/evidence examples from `ProfileSpec`; reconcile reuses normal builder; installed coding context had `code_claim` and no analysis leakage. |
| PF-REAL-015 | Profile gate matrix conflicted with advertised evidence vocabulary. | Coding accepted failing tests as execution proof; math used unadvertised `proof_check` and over-blocked any `counterexample`. | Coding requires successful passing/execution evidence; math advertises `proof_check`; resolved/negative counterexample searches do not block; gaps include diagnostics. |
| PF-REAL-016 | Copy installer included private `worknotes/` in installed plugin. | Installer excluded caches/reference/tmp but not worknotes. | `worknotes` added to install exclusions; copy-install test covers absence; installed smoke confirmed `installed_worknotes=false`. |
| PF-REAL-017 | Agents can call the CLI with wrong/missing arguments and need compact next-step guidance. | Error envelope returned JSON but usually only `error`; file-not-found and JSON parse failures exposed raw Python/JSON messages without actionable hints. | CLI now adds short `hint` fields for missing/invalid command args, profile choices, run ids, JSON object/file errors, and verifier target mistakes; source full suite `48 passed`; installed CLI contract `5 passed`; installed wrong-arg smoke verified hints. |
| PF-REAL-018 | Real run `pf_run_7474232e15cf` reached `verify --mode independent_review` after default 8x4 planning and 40 child assertions. The generated reviewer `delegate_task_payload_suggestion` had 40 leaf tasks, but each reviewer context only contained profile marker, target assertion id, producer task id, and generic instructions; it did not include the assertion text's evidence items, artifact metadata, or report summary, and duplicated `[PONDER_FORGE_PROFILE=analysis]`. | `verifier._payload()` serialized only thin `task['context']`; `create_reviewer_tasks()` did not embed the visible assertion/evidence/artifact bundle needed by an independent reviewer. | CLOSED. `verifier.py` now builds rich reviewer context with assertion, producer report, evidence, and artifacts; `_payload()` has one profile marker; existing thin tasks refresh through `PonderForgeStore.update_task_context()`. Source tests `70 passed`; installed tests `70 passed`; old run refresh smoke confirmed rich context and one profile marker. |
| PF-REAL-019 | Installed real round `clean1_post_pf018` / run `pf_run_5e634c37434c` completed start/plan/delegations/report/verify verdict recording. Gate passed, but `status_after_verdicts` returned `next_required_action=delegations` with 88 agent tasks because 40 reviewer tasks remained queued. | `record_independent_verdict()` stored accepted verdicts and updated assertion status, but never marked the reviewer task as finished. `cmd_status()` then saw queued reviewer tasks before finalize routing. | CLOSED. `record_independent_verdict()` now marks the reviewer task finished when it records a verdict for a task in the same run; CLI regression checks `status_after_verdict.next_required_action == finalize`; source and installed full suites `70 passed`; installed real rounds `clean1_post_pf019`, `clean2_post_pf019`, `clean3_post_pf019` all completed with quest unchanged. |
| PF-REAL-020 | A late real orchestrator subagent result for `pf_run_7474232e15cf` / `pf_task_4a9b2e5df0c9` returned valid JSON with 5 child reports and required evidence groups, but top-level `artifacts` was a metadata object instead of an array. Direct `submit-report` would fail in ingest; the old diagnostic run already had a report for that task, so the late result was validated but not submitted to avoid duplicate reports/assertions. | `_lane_report_contract()` required the top-level `artifacts` key but did not explicitly state array shape for the lane's own artifacts; `_normalize_payload()` assumed iterability and would surface an unclear Python type failure for dict artifacts. | CLOSED. Lane context now states top-level `artifacts` must be a JSON array and to use `[]` when none; ingest raises `artifacts must be a JSON array`; CLI returns a short actionable hint; source and installed suites `71 passed`; installed bad-artifacts smoke returned the expected hint; installed real rounds `clean1_post_pf020`, `clean2_post_pf020`, `clean3_post_pf020` all completed with quest unchanged. |
| PF-REAL-021 | User challenged the prior “all functions” claim. Audit confirmed post-PF-REAL-020 clean rounds exercised installed CLI/state/gate/finalize with controller-generated reports, plus one late live lane result, but not the full live subagent lane coordinator + nested child + independent reviewer chain for three consecutive real-task rounds. | Stability proof gap: the plugin's agent-facing contracts were not repeatedly exercised by actual Hermes `delegate_task` lane/reviewer agents after PF-REAL-020. | OPEN. Plan written at `worknotes/2026-07-04-pf-real-021-live-subagent-stability-plan.md`. Live round 01 lanes were generated by installed Ponder-Forge, executed by live Hermes subagents, collected as 8/8 valid JSON reports, and submitted through installed `submit-report`; 83 exact generated live reviewers have been dispatched and are pending. Next action is collect/record reviewer verdicts, gate/finalize/reconcile, then repeat until three clean live rounds pass or a plugin issue is reproduced and fixed. |
| PF-REAL-022 | Live round 01 dispatched 8 exact generated lane coordinator orchestrator tasks as `deleg_edd050a1` for run `pf_run_80cb097d3870`. A 9-attempt / ~9-minute collector wait found 0 lane reports; delegation cache still contained only old summaries; visible process scan did not show obvious lane subagent processes. The async batch later returned all 8 valid lane reports after ~22 minutes. | Bounded polling was too short for this nested live delegation shape; no Ponder-Forge report-loss bug was reproduced. The remaining risk is delayed observability/long-tail runtime rather than missing results. | NON-BLOCKING RISK. Evidence recorded at `worknotes/real_subagent_stability_2026-07-04/live_round_01/10_wait_timeout_analysis.md`; `11_watchdog_result.json` records that the watchdog was superseded by async completion. No code change required unless reviewer batches show actual loss or contract failure. |

## Final proof anchors

- Final clean summary: `worknotes/real_cli_rounds/post_pf016_clean_summary.json`.
- Worker synthesis: `worknotes/real_cli_rounds/round1/worker_reports_final_synthesis.md`.
- Final pushed implementation commit: `38b926e (HEAD -> main, origin/main) fix: harden ponder-forge real-run contracts`.
- Installed copy at final closeout: `/home/xu/.hermes/plugins/ponder_forge`; command + bundled skill only; no direct Ponder-Forge tools/hooks by default.

## Deliberate non-blocking boundaries

- No separate `submit-report --schema` command was added; schema is carried by `delegations` and bundled skill.
- Submitted artifact paths remain metadata; Ponder-Forge does not copy arbitrary external artifacts into `$HERMES_HOME`.
- Broader research/design gate policy tightening was deferred because no reproduced real-task failure required it.
- Long-running daemon/multi-controller concurrent write behavior was not part of this real-run stability proof.

## Compression note

The previous verbose ledger is recoverable from git history before this compression. This file now keeps only action-critical issue status, root cause, fix, and proof anchors.
